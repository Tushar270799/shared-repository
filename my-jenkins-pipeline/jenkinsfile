pipeline {
    agent {label 'host'}
    stages {
        stage('Build') {
            steps {
                sh 'whoami'
            }
        }
        stage('Test') {
            steps {
                echo "Test ongoing"
            }
        }
        stage('Deploy') {
            steps {
                echo "DEPLOYMENT COMPLETED AGAIN at night.... !!!!"
            }
        }
    }
}
