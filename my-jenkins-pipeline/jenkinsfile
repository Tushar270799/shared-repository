pipeline {
    agent {label 'host'}
    stages {
        stage('Build') {
            steps {
                echo "build ongoing"
            }
        }
        stage('Test') {
            steps {
                echo "Test ongoing"
            }
        }
        stage('Deploy') {
            steps {
                echo "DEPLOYMENT COMPLETED AGAIN at night.... !!!!"
            }
        }
    }
}
